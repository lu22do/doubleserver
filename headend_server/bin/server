#!/usr/bin/env node
var debug = require('debug')('headend_server');
var app = require('../app');

var port = 3000;

app.server.listen(port, function() {
  console.log('Listening on port ' + port);
});